# MDM Identity 接入

本项实现管理员身份消费、静态资源权限与资产查询。账户、本地密码、联合 IdP、JIT、中央会话由 rss-identity 拥有。运行证据以 #2343 的 PR 和受测 HEAD 为准；本文不表示候选或产品 T3 已通过。

## 唯一应用与依赖

`rss-mdm-app` 输出唯一生产 binary `rss-mdm`，提供 `serve --config FILE` 和 `migrate --config FILE`。
示例 binary 已更名 `rss-mdm-fixture`，只保留 ingest-fixture/project/inspect；旧 migrate 命令不保留。
产品不依赖 examples，迁移顺序只由产品应用维护。

RSS 主仓依赖固定同一完整 Git revision；Identity client/contracts 另固定 rss-identity 的完整 revision，精确值由 Cargo.toml/Cargo.lock 持有。CI 分别核对两种来源及公开包集合，不按 rss-* 前缀放宽来源。OIDC 复用 openidconnect；在线验证复用 IdentityClient，不存在自研 wire verifier、JWT 离线认证或认证缓存。

## 安装与运行配置

部署 owner 预建 mdm_owner、mdm_runtime、mdm_api 登录角色并分别配置密码。均不得是 superuser/BYPASSRLS 或通过角色成员资格取得更高权限。mdm_owner 持有目标数据库和 public schema CREATE；迁移依次安装 Observation、Projection、Inventory v1 与只读角色授权。既有 SQL 摘要不变；新 reader 授权只允许 mdm.inventory SELECT。Inventory 查询使用 mdm_api，worker 使用 mdm_runtime；#2347 新增独立 mdm_access 写角色和必填 access_database，见 [F02 升级指南](202609090001-2347-enrollment-audit.md)。reader 启动检查拒绝 Inventory 以外的用户数据表访问（包括迁移账本）、用户序列权限和用户函数执行权限；PostgreSQL 自身目录查询保持可用。

迁移配置只有 database 对象：host、port、name、user、password_file、ca_file；使用 mdm_owner。服务配置示例见 [配置样例](../../fixtures/mdm-config.example.json)，使用 mdm_api。配置与秘密为普通 0600 文件，禁止最终路径 symlink；CA 为可读取 PEM。示例中的域名和文件路径必须由部署 owner 替换，不是生产默认。

```sh
cargo run --locked -p rss-mdm-app -- migrate --config /private/mdm-migration.json
cargo run --locked -p rss-mdm-app -- serve --config /private/mdm.json
```

服务仅绑定回环地址，必须使用 [受控入口配置](../../deployment/nginx.conf) 的等价规则提供 HTTPS，并保留配置中的精确 Host；不要开放直连后端或增加绕过限流的入口。该profile直接接收客户端TCP连接，以真实peer而非Forwarded/X-Forwarded-For做每IP 20次/分钟、burst 10限流，超过返回429；每peer最多8个活动请求连接、每server最多128个，读取请求头/body与发送响应期限均为10秒。429统一返回request_limited；代理自身产生的HTTP拒绝（包括413）同样包含no-store、no-referrer、nosniff。代理仅记录生成的request_id、可信上游request_id、status和时延，不记录URL、header、body；应用与代理日志均为运维事件，不是持久业务审计。多级代理须另行定义可信来源，不直接沿用该预算。每浏览器最多4个未完成登录，不能占满全局pending。`product_origin` 必须与 Identity 注册的 product origin/callback 一致；OIDC issuer 使用已注册的公共 issuer。`identity.origin` 是后端可达的私有验证 HTTPS 入口，`identity.issuer` 是 OIDC 公共 issuer；分别保证 DNS/路由/CA 正确，不通过反代重写 issuer。私有验证端点只能向已注册产品服务暴露，公有 OIDC 端点必须可被后端访问；不得让私网 DNS 路由遮蔽 OIDC discovery/token/JWKS。

配置由消费式compile入口一次验证并生成Policy，生产生命周期与独立Router复用同一编译产物；改角色、设备范围或来源后重启。MDM与OIDC公共authority必须使用不同cookie hostname，不能仅靠不同port隔离；metadata/JWKS在启动时获取，新登录遇到未知签名key拒绝，签名key轮转后重启刷新公钥。ID token本地算法策略固定RS256，拒绝HMAC/none发现配置；claims与at_hash共用受限verifier，client secret仅用于OAuth客户端认证。当前为单实例；重启清空本地会话，浏览器持旧 cookie 时开始登录会清理旧句柄并重新认证。无角色绑定的用户可经 /api/v1/auth/me 获取自己的 pairwise subject，由 MDM 管理员写入映射；Identity 管理员不会自动获得 MDM 角色。

## 静态授权

每个binding必须填写tenant_id、client_id、subject、roles、devices、allow_wipe、allow_enrollment、allow_manage_credentials；后者规则见 F02 指南。tenant/client必须等于本进程Identity配置；subject从本人已验证的auth/me取得，不能用email或Identity内部principal UUID代替。
roles只接受super_admin、mdm_admin、security_admin、help_desk、auditor；没有默认角色。devices为精确设备ID集合，或唯一元素"*"明确授权本tenant全部设备，不能将"*"和其它设备混写。样例只读占位binding须替换后使用；启动后若暂不分配权限，可明确设置bindings为空。
危险wipe必须同时包含super_admin或mdm_admin角色、设备范围、allow_wipe=true；其它角色配置allow_wipe=true在启动时拒绝。该许可当前只得到501，不表示执行能力已实现。

## HTTP 契约

| 方法 / 路径 | 行为 |
| --- | --- |
| POST /auth/login | 精确 Origin、X-MDM-Request: 1；已有有效产品会话另需 X-CSRF-Token。返回 authorization_url。 |
| GET /auth/callback | 绑定浏览器的单次 state；Code+PKCE、ID token 与在线验证通过后旋转 cookie，303 到 /api/v1/auth/me。HEAD 不执行兑换。未知OAuth响应扩展参数被忽略，已知字段重复仍拒绝。OAuth服务异常与未知错误返回503，不误报身份失效。 |
| GET /api/v1/auth/me | 每请求在线复核，返回 subject/tenant_id/client_id/roles/csrf_token/expires_at；不返回 protocol token。 |
| POST /api/v1/auth/logout | Origin、X-MDM-Request: 1、CSRF；删除本地产品会话并清 cookie。Identity 不可用时仍可退出；不是中央 logout-all。 |
| GET /api/v1/devices/{id}/inventory?channel=…&source=… | 五种既定角色及显式设备范围；由服务端有效注册映射选择来源坐标，不合并来源/注册世代；响应字段由MDM持有（tenant_id/device_id/registration/source/epoch/coverage/fields），不暴露RSS Scope wire。不接受 tenant/registration/epoch 参数，无数据404。 |
| POST /api/v1/devices/{id}/actions | 额外要求 super_admin/mdm_admin、allow_wipe=true、设备范围、Origin/请求标记/CSRF；只识别 {"action":"wipe"}，无权403，有权501 action_not_supported。 |

所有响应 no-store/no-referrer。无效身份401，权限/CSRF拒绝403，输入错误400，依赖/服务凭据/协议错误503。响应和日志不输出 SQL/provider 原文或凭据。默认请求预算10秒、在线验证5秒，body最多16KiB、URI最多8KiB。全局最多4个昂贵请求（业务与code exchange共用），单会话最多1个业务请求；在在线验证前try-acquire，无排队、满载503，完成/取消/超时自动释放。本地退出不占昂贵请求槽。调用方应限制并发并在503后退避重试；不会自动重试code exchange。

Cookie为 `__Host-mdm-session`，Secure、HttpOnly、SameSite=Lax、Path=/、无Domain。浏览器只保存随机句柄；协议凭据存有界内存。最多10000个会话、1000个待完成登录（每浏览器最多4个），登录期限300秒，容量满拒绝新建、不驱逐有效会话。

每个业务请求调用SDK得到当前VerifiedIdentity，MDM policy构造请求级授权凭证后才访问reader。handler不能取得PG pool或构造裸tenant查询。reader在同一事务设置RLS tenant并限定完整scope。租户身份来源仅为可信结果，所有调用失败均不得沿用上次身份。

Identity失效提交后开始的验证拒绝；已验证在途请求不追溯取消。MDM调用不续中央idle，不自动刷新token；到期重新授权。静态角色变更通过重启生效。危险动作501不会产生命令、Outbox或执行成功审计。

## 验证与后续

`make test` 覆盖组件；`make t2` 包含真实TLS PG、正式迁移、精确读取/隔离和权限漂移。`MDM_IDENTITY_CANDIDATE=/absolute/candidate make t2-identity` 必須提供与 fixtures/identity-candidate.json 完全匹配的真实候选，校验OCI归档摘要，使用其Identity binary与固定PG/Hydra运行MDM Router测试。候选使用清单规定的linux/amd64镜像；PG/Hydra等provider使用同一固定多架构摘要的本机平台，实际平台写入验证结果。测试拓扑是临时接缝，不能替代部署模板、完整浏览器或真实设备T3。CI不会隐式构建/替换候选，缺少输入明确失败。

组映射归 [#2363](https://dev.azure.com/shengming0923/rss/_workitems/edit/2363)，部署浏览器T3归 [#2364](https://dev.azure.com/shengming0923/rss/_workitems/edit/2364)。注册许可和持久审计由 [#2347](202609090001-2347-enrollment-audit.md) 接入，业务请求处理 8 秒并预留 2 秒审计收尾。RSA公钥验签的独立限定接受与退出归 [#2365](https://dev.azure.com/shengming0923/rss/_workitems/edit/2365)，不是漏洞修复声明。

来源：Identity #2331/#2336 协议；openidconnect 4.0.1 client.rs / verification/mod.rs 的 Code+PKCE 与算法限制；[RFC 6749 §4.1.2](https://www.rfc-editor.org/rfc/rfc6749#section-4.1.2) 的响应扩展处理；Axum axum-v0.8.9 middleware/from_fn.rs；固定RSS runtime/managed listener 公共接口。历史 WinMDM src/cmd/api/main.go:548–587 仅用于角色目标溯源，不恢复其本地JWT角色授权。

## 排障分类

配置读取报告私有文件路径，JSON错误报告行/列；listen、数据库角色/地址/秘密/CA、origin、issuer、tenant、client、binding、OIDC算法分别报告闭合字段类别，不打印配置值。每次请求生成x-request-id并记录结构化status/latency与内部原因，Identity server失败附官方correlation_id；HTTP仍只返回安全码。SDK未细分的不可用结果记录identity_validation，不另写协议解析器猜测原因。启动按configuration、database_configuration、reader_connection_or_admission、identity、listener_bind、total deadline区分阶段；关闭区分signal、resource drain与critical task退出。迁移错误保留unit与phase，changed/interrupted或completion unknown需要检查安装状态，禁止删除ledger强行重跑。数据库/Identity底层原文不回显。
