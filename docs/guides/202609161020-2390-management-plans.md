# 授权管理 API 与持久计划

#2390 在现有 `rss-mdm-app` 中组装 Group、Scope、Policy、Resource 和软件发布。
核心与对应 PG adapter 保持独立，应用拥有真实管理员权限、资产来源解释、跨 owner 引用和事务审计。
这里只生成计划和后端源发布事实，不向设备派发，不产生 Applied/Converged 或安装成功证据。

## 配置与升级

使用 `fixtures/mdm-config.example.json` 的当前格式。每个 binding 必须显式提供 `management` 动作集合；空集合拒绝全部新管理动作。
角色名称和旧设备白名单不自动赋予管理权；管理动作是同租户的全范围能力，不按操作者设备白名单截断共享组。
只有管理权限的 binding 可配置空 devices；这不会赋予设备注册、撤销或原有资产 API 权限。

管理连接 `mdm_management_runtime` 与外部发布连接 `mdm_software_driver` 分离；两者都必须连接同一产品数据库。
`management.target`、`lineage`、`epoch` 必须与现有 RSS storage lineage / tenant epoch 相符，由部署 owner 明确设置，不能从请求推导。
管理角色使用 SERIALIZABLE，完整设备集合与缺失字段共享同一个数据库快照；并发更新引发的事务失败按原操作身份重试。
运行角色不得拥有 DDL、超级用户、BYPASSRLS 或其它业务凭据读取权；启动检查 schema、RLS、实际权限和组件 catalog。
资产与注册只读事务不请求行写锁，也不授予 UPDATE 权限；管理对象的引用保护依赖 SERIALIZABLE 与 owner 提供的事务接缝。
启动总预算为基础 15 秒，加发布连接 5 秒和每个配置源 6 秒；最多 16 源，来源连接失败保留独立诊断类别。
连接取得后立即注册到现有生命周期，listener 关闭后再关闭池；启动失败不能进入 readiness。

新部署先安装现有 `crates/app/schema/software-publication-roles.sql` 和新增 `crates/app/schema/management-roles.sql`；升级部署只安装新增角色脚本。
由外部管理员配置登录和独立密码，然后使用 `mdm_owner` 执行 `rss-mdm migrate --config <migration.json>`。
迁移追加 Group 和 management 两个单元，总共十六个；既有单元摘要不改写。设备标识约束统一为 1–256 UTF-8 字节、不含控制字符。
已有持久数据保留；超出新约束的数据导致升级明确失败，不自动删改数据。旧配置必须显式更新，不存在兼容默认或双路径。

`management.sources` 是显式源配置列表；空列表表示没有被配置的私有源，源管理请求返回不存在。
每项提供 name、test/pilot/production 三个独占物理源、公开产物 origin/IP/CA 和 max_artifact_bytes。
WinGet/Brew 的底层源格式见[发布指南](202609132008-2388-2389-backend-persistence.md)。HTTP 请求不能添加地址白名单或提交源凭据。

## HTTP 契约

所有路径前缀为 `/api/v1`，使用现有会话、在线身份验证、Origin/CSRF 和 16 KiB 请求体上限。
路径中的 ID 必须正确 URL 编码。写入统一为：

```json
{"operation_id":"非零UUID","expected_revision":1,"input":{"action":"..."}}
```

operation_id 绑定 tenant、真实操作者、client、对象和完整输入。提交未知时重发原请求，不更换 ID 或预期版本；更改输入返回 409。
响应包含 `x-request-id` 供审计关联。写入成功审计与业务状态同事务；查询/拒绝审计失败返回 503，不返回未审计的数据。
401/403 沿用既有身份与权限错误；400/422 为无效输入；409 为版本、引用、不可变身份或状态冲突；503 为依赖失败或提交未知。

| 路径 | 能力 | 权限 |
|---|---|---|
| GET/POST `/groups/{uuid}` | 读取定义/成员；create、edit、rule、members、delete、recompute | group_read / group_write；重算为 group_recompute |
| GET `/groups/{uuid}/preview?expected_revision=N` | 当前可信资产规则预览和 snapshot 指纹 | group_read |
| GET/POST `/scopes/{uuid}` | 读取；put、delete，定义版本 CAS | scope_read / scope_write |
| GET/POST `/policies/{id}` | 读取；create、activate、pause、resume、archive | policy_read / policy_write |
| POST `/policies/{id}/previews` | 解析 Scope 并冻结计划预览 | plan_preview |
| POST `/policies/{id}/plans` | 校验前置条件并保存指定预览 | plan_save |
| GET `/plan-previews/{uuid}` | 读取原始快照、来源解释与计划 | policy_read |
| GET/POST `/resources/{id}` | 读取版本目录；create、version、activate、deprecate、archive | resource_read / resource_write |
| GET/POST `/software-sources/{source}/candidates/{id}` | 读取；candidate、validate、approve、authorize、publish、recover、retry、withdraw | release_read；其余动作分别对应 release_write/validate/approve/publish/recover/withdraw |

Group create 提供 name、description、criteria；criteria=null 创建静态组。静态 members 提供 add/remove，不允许改动态成员。
规则的首期 AST 提供 eq/ne/contains/not_contains(field,value)、in/not_in(field,values)、and/or(children)，字段仅 `device.model`、`device.os.version`；不接旧表达式、SQL 或扩展采集字典。
动态组先 preview，再以相同预期 revision 和返回的 snapshot 调用 recompute。重复操作返回原回执；资产或注册变化使旧 snapshot 失效。
重算使用当前启用 `mdm.windows` 来源的设备集合，资产按注册世代的真实 Observation Scope 定位，再归属稳定 Device。
使用 Inventory owner 持有的 last-good 值与来源时间，不根据客户端自报值或旧世代数据求组；缺字段为 Unknown，不伪造成空字符串。

Scope put 的 definition 为 targets、limitations、exclusions；每项引用为 `{ "kind":"group|device", "id":"..." }`。
limitations=null 表示不限制，[] 表示空限制。最多 1000 个来源、累计 10000 个展开成员；超限整体失败，不静默截断。
来源绑定 Group 聚合 revision，另存 member_version；初始空组也有合法来源版本。Scope 和组引用的变更/删除在同一受控事务中序列化。
被计划历史引用的 Scope 不能删除；被当前 Scope 或计划历史引用的组不能删除。设备撤销属于安全动作，不被 Scope 引用阻止；失效设备不能生成新有效目标。

Policy activate 指定 version、resource、resource_version，载荷摘要从被冻结/激活的真实资源版本解析。
预览 input 为 scope UUID 和 expected_revision（与外层一致）；保存 input 为 preview UUID。
预览持久化本身不选择当前计划；保存时重新验证 Scope、Group、设备的实际注册 ID/通道/generation 及 Policy CAS，并原子执行 SelectTargets、Replan、引用记录和审计。
来源变化返回冲突，不能偷偷重算为另一份计划；事实读取上限 1000，存在后续页时整体拒绝。
每个预览保留实际来源/版本/时间、目标、解释和意图；`dispatch=not_requested` 明确未派发，修改当前组不重写历史。
Resource version 输入包含 kind、不可变 version 和完整 variants。归档同时检查发布引用与 Policy 历史引用，不能由客户端声明零引用。

## 审批与外部提交

候选引用真实资源版本及 Submission；服务端验证完整 WinGet/Brew 内容和公开产物摘要。
审批动作指定 ring 和 publisher_subject（本产品已配置 release_publish 的主体）；审批人由当前可信会话确定。
审批/发布核心记录 `(client,subject)` 的规范身份，默认禁止同一管理主体审批并发布；DTO 中不存在可覆盖当前操作者的字段。
固定服务审批人/发布人已经移除，受控后台身份仅用于源执行和对账。旧服务账号审批不能为新的管理员身份提供发布授权。

每环 validate → approve → authorize 后，publish/recover 指定 ring、实际 publication 摘要数组和 attempt。
服务端在外部操作前保存原请求及固定时间的审计意图；外部提交成功、本地回执未知时按原 publication/attempt 对账，不生成另一份发布。
HTTP 返回当前候选视图，重放不会再次发起已确认提交。`published` 只表示已确认源元数据与批准内容一致，不表示客户端安装。

## 验证与证据

- `python3 hack/management-t2.py`：正式迁移、最低运行角色、重放、CAS、引用、审计回滚和准入漂移。
- `python3 hack/identity_t2.py`：固定 Identity 候选＋真实 PG＋真实 HTTP 管理闭环，包含独立审批人与发布人及 HTTPS 私有源。
- `python3 hack/publication-t2.py`：既有真实 PG、HTTPS、Git 发布/撤回/提交未知恢复。
- `make ci`：提交受测源码后一次执行所有本地 gate，绑定 HEAD、RSS/Identity revision、独立消费者 lock 和未覆盖项；不运行父仓 CI，不创建远端 CI。

运行 Identity 验证需设置 `MDM_IDENTITY_CANDIDATE` 为与 `fixtures/identity-candidate.json` 精确匹配的已批准目录。
没有真实设备验证；不得把这些后端证据当作 T3。

ref: axum axum/src/routing/mod.rs@c59208c86fded335cd85e388030ad59347b0e5ae
