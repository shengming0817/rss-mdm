# Policy / Resource / 软件发布持久化（#2388 + #2389）

## 能力与 owner

三个独立 package 为 `rss-mdm-policy-postgres`、`rss-mdm-resource-postgres`、`rss-mdm-software-release-postgres`。它们只依赖对应核心与必要 RSS/PG，不读取其他业务表。`rss-mdm-app::software_publication` 负责映射、引用、服务身份、源目标、公开产物和成功审计。

所有 Store 借用宿主持有的同一个 `Arc<PgRuntime>`；宿主负责关闭。独立运行角色通过 verify-full TLS 连接，禁止 owner/superuser/BYPASSRLS/DDL/权限转授。构造 Store 校验精确 schema catalog、约束、索引、RLS 和有效权限。app 组合 admission 还拒绝无关业务表/序列/函数权限、schema CREATE、audit grant option 和宽松 audit tenant policy。`*_in` 校验 runtime owner/tenant，永不自行 commit；调用方必须处理业务拒绝并将外层 PG 错误返回给事务 owner。

Resource/Release 的 Outbox MessageId 使用原 RequestId 的固定长度 SHA-256 编码，以容纳核心允许的斜线等字符；业务回执和事件载荷保留完整原身份。

持久文档只支持当前格式，字段数量、闭合枚举、摘要和核心构造器共同校验；无历史兼容读取、path patch 或另外的 Outbox。

## Policy

`Request` 使用核心 RequestId、expected_storage_revision 和固定 as_of。Create / Transition 管理策略生命周期；RecordExecutions 独立记录真实执行受理和进度。同身份异内容拒绝，未知提交沿原身份读回。

候选依次调用 `begin_candidate_in`、`append_candidate_targets_in`、`seal_candidate_targets_in`、`advance_candidate_facts_in`。
输入页最多 1000 对象、16 MiB，当前目标最多 1,000,000。多版本执行历史沿 `(version,device)` 逐页读取，不按目标容量截断。
Policy 核心只处理单目标或单事实，规范流式摘要不含分页边界。目标、各类意图和替代关系分别分页，完整结果由候选状态表达。

`save_candidate_in` 校验策略 CAS 和全部引用令牌后切换当前计划指针。保存只安装意图，不创建 Planned 或任何执行事实。
应用在同一短事务校验权限、来源身份并更新最近保存 Scope 绑定和审计。源结果发布原子推进 Policy adapter 的引用令牌，旧计划立即过期；A→B→A 不恢复旧引用。
旧 SelectTargets、ReplaceFacts、Replan、完整快照/计划 blob 和解码路径已退出。接口示例与恢复语义见 [Policy adapter](../../crates/policy-postgres/README.md)。

## Resource 与完整版本发布

Resource 的 Version 内容单独存为不可变记录，聚合只保存生命周期引用。`Resource::snapshot/restore` 校验租户、资源、kind、唯一 Active 和状态结构。ResourceStore 提供 execute、version、get、operation 及借用事务入口；Archive 只能在伴随事务中执行。app 先锁版本并检查所有持久候选引用；被历史候选引用的版本也不归档。

一个 Candidate 是完整平台包版本。SoftwareIdentity 为 source/package/version/platform，Content 的 VariantContent 持有全部 architecture/variant 和产物闭包。V2 摘要直接替换旧单 variant 输入；没有旧发布接口或格式 shim。

ReleaseStore 保存候选、验证/审批、原请求/回执和每次发布尝试历史。相同软件版本的描述、manifest、variant 和产物不可变；源配置快照独立绑定审批。重放只返回历史 receipt，不返回 Publish，不回退当前状态。

## app 使用顺序

1. 以受控进程配置构造 ArtifactReader、具名 RingSources(test、pilot、production) 和 ServiceIdentity，再 `PublicationService::connect`。物理源全局独占，绑定唯一逻辑 source 与固定 ring；不允许另一个 tenant 或逻辑别名共享同一外部命名空间。后台身份匹配 tenant；N12 管理入口把在线验证的实际操作者传入用例，不再配置固定服务审批人/发布人。
2. ResourceStore 创建资源及冻结版本。CandidateInput 明确资源版本、预期资源 revision、候选/请求身份、固定时间和 Submission。
3. `create_candidate` 在事务外读取产物，事务内重验资源和完整平台集合，原子保存候选、引用、软件版本 authority 与成功审计。
4. `validate` → `approve` → `authorize` 使用 ServiceRequest(actor、id、expected_revision、as_of)，approve 额外指定经产品授权的 publisher。每环重新验证审批，后环要求前环已 Published；保留原请求用于重放。
5. 授权事务固定 PublicationId、attempt、目标字节/摘要、Brew base/target commit 和源互斥槽。提交后 `publish` 才记录可能已调用并执行一次外部写。
6. `reconcile` 只读取原目标并记录核心结果；外部成功、本地失败/未知时继续使用原 PublicationId/attempt。`candidate` 和 ReleaseStore::operation/original_request/attempt_history 可查证结果。

只有信息预检阶段可证明未发送写请求的失败才记录 NotApplied、释放互斥，并允许 `retry` 创建同 PublicationId 的下一次尝试。POST 阶段超时和未知结果只能继续对账，不按 404 重试。

WinGet 按完整 package/version 互斥，只 POST，不通过 PUT 覆写冲突。写身份使用 x-functions-key，读取信息与完整版本核对真实源及批准内容。Resource variant key 通过 `winget_variant(Query)` 得到，包含 installer type、scope 和显式 installer ID/无 ID；必须覆盖全部 installer。

Brew 每环使用独立专用 bare Tap，同一 Tap 的 main 修改串行。Cask 映射完整架构集合；Formula 映射全部 Bottle、源码产物与调用方明确声明的依赖快照/产物。依赖声明由受控调用方 attestation，当前不发现依赖或运行 Ruby。Bottle URL 对照固定上游 Filename URL 编码（rebuild=0）；所有实际读取对象均验证长度与 SHA-256。

## 撤回与未知

withdraw 使用调用方稳定 ServiceRequest 先隔离候选，关闭新授权；尚未调用的发布可在同一受控事务证明 NotApplied。已可能调用且结果未知时撤回保持 Pending，禁止先删除后让迟到 POST 重现。

撤回请求和调用目标分开保存，目标只可 INSERT。WinGet 仅删除精确版本；Brew 在最新已核实 head 上删除仍由该发布持有的定义，保留其他包和旧 commit。旧版本已经被新版本取代时撤回为 no-op，不回退分支。

`reconcile_withdrawal` 只查询已调用的原目标，不发起新的删除；尚未调用的撤回由显式 `withdraw` 继续。WinGet 1.0 没有操作查询：确认 204 加版本缺失可完成；DELETE 回包丢失后即使看到 404，仍保留 Pending 和互斥，不自动重发或释放。信息预检明确未发送 DELETE 时，以成功审计记录该事实，允许显式 withdraw 再调用同一固定目标；仅查询的 reconcile_withdrawal 不重试。需要源 owner 提供终止/对账能力后独立接入，当前不提供未经证明的强制成功入口。Brew 可用已进入分支历史的删除 commit 对账。

旧 commit、公开产物与历史引用都保留。本期无 GC、缓存即时撤销、自动回退或终端卸载。重新发布相同版本须先查清全部旧尝试并完成撤回，再建新候选、重新审批，字节仍须相同。

## 产物与权限

产物仅为已上传的公开 CDN/S3 稳定 HTTPS 对象。匿名 GET，不发送 Authorization、Cookie 或源专用头；拒绝内嵌身份、query/fragment、重定向和隐式代理。origin/IP/CA 仅来自受控进程配置（私有 CA/IP 用于受控 T2，生产 CDN 可使用系统公开根），Submission 不能扩展该允许集合。显式批准 origin/IP/CA，保留证书和主机名验证；读取为流式、有界时间/长度并校验 SHA-256。公开对象可以被持有链接的人读取，RLS 只保护资源、审批、来源、调用和审计元数据。

无上传、私有下载授权、签名 URL 或通用存储 provider。下载授权以后独立设计，不以假 credential reference 代表匿名访问。

## 迁移与验证

当前部署按 #2437 使用全新 PostgreSQL 17，MDM 与内嵌 Identity 共享产品数据库，并使用各自最低角色。管理员先创建 mdm_owner 等基线角色，再按顺序安装 `software-publication-roles.sql`、`management-roles.sql`、`identity-roles.sql`（位于 `crates/app/schema/`），配置各角色独立登录秘密。`rss-mdm migrate` 以 mdm_owner 安装 固定摘要单元（以二进制 --describe 为准），包括完整 RSS schema、业务表、追加权限与 Identity v11；运行角色不执行 DDL。支持空库安装与精确重放；旧清单、损坏或不完整账本拒绝且保留数据。升级见[命令服务前向升级](../deployment/202609210000-2465-command-upgrade.md)。安装输入和初始化见[认证指南](202609091600-2343-mdm-identity.md)，生产源部署不属于本批 T2。

```sh
make test
make t2-backend
make t2-publication
# 已提交且 clean 后，以固定源码 SHA 逐包消费
make backend-consumers
make ci
```

backend-t2 使用固定摘要 PG、verify-full TLS、隔离角色和真实 COMMIT 回包丢失代理；publication-t2 使用真实 PG/HTTPS、有状态 WinGet 兼容协议 fixture 和 bare Git。缺依赖或未执行必要测试明确失败。独立消费者按 default/no-default 获取固定 Git SHA，分别核对普通/构建闭包、实际 feature tree、独立 lock/target 和真实 PG 行为，不能由测试直接依赖补齐 adapter features。

结果绑定 PR 受测 SHA、RSS pin、lock 和实际命令输出；源码消费不是 registry 发布，协议 fixture 不是生产源部署，T1/T2 不是端侧 T3。没有独立性能 SLO，本项 benchmark gate 不适用。

## 源码参考

- SQLx v0.9.0 `sqlx-core/src/transaction.rs`：显式 commit/rollback 与 Drop 不能证明提交结果。
- Microsoft winget-cli-restsource `21cd5dda3dab39aa059f4d34914959736af7ee70` 的 `documentation/WinGet-1.0.0.yaml`：完整版本发布和精确版本删除。
- Git v2.51.0 `refs.c`：old OID 参与 ref transaction CAS。
- Homebrew/brew `4ef2edc123233e30f94c4a6d21bcb019806f1945` 的 `Library/Homebrew/bottle.rb`：Filename URL 编码，固定规范不调用客户端。


## PR 1020 诊断、审计与撤回查询

PG adapter 在 RSS `PgError` 的脱敏边界之前记录 `stage/kind/reason`，JSON 错误只记录分类与位置，
不记录原始 JSON、SQL 参数、地址或凭据。事务结果仍由 RSS commit/rollback 确定性承载。
应用层 `software_publication::Error::Diagnostic` 保留操作阶段、业务分类与脱敏原因，调用方不得
把诊断失败解释为“外部写未发生”。参考 Rust `core::error::Error::source` 与 Tokio tracing
的 poll-scoped instrumentation；不持有 span enter guard 跨 await。

`mdm_access.audit.software` 记录软件操作的 operation、publication、attempt、ring、binding digest、
stage 与 outcome。软件源 binding 本身包括凭据 reference 的配置摘要，审计不保存 key、manifest、URL 或仓库路径。
`unknown/attempted` 映射为 audit `unknown/202`，`not-applied` 为 `failed/200`，确认结果为 `success/200`；
HTTP 状态字段表达该本地记录的处理结果，不是外部源的原始 HTTP 响应。审计和业务状态在同一事务提交。

`PublicationService::withdrawal_status(publication, attempt, cutoff)` 只读取持久状态，不调用软件源：

| 返回值 | 含义与下一步 |
| --- | --- |
| `None` | 此 attempt 尚无撤回意图 |
| `WaitingPublication` | 先 reconcile 发布结果 |
| `PreflightRetryable` | 尚未提交源写，可显式重试 withdraw |
| `SourceOutcomeUnknown` | WinGet DELETE 回执未知，需要 source owner 核对，不能盲目重发 |
| `AwaitingConfirmation` | 已确认接收或 Git 可核对提交，继续 reconcile withdrawal |
| `NotPublished` | 发布确认未应用，等待本地撤回意图收尾，无需外部删除 |
| `Complete` | 撤回已持久完成 |

以上查询支持服务重启后继续使用同一 publication/attempt；候选 quarantine 的 request identity
与外部 publication/attempt identity 分别记录。N12 可消费这些状态，不应再把全部未完成情况显示为 Pending。

source T2 必须执行明确的 WinGet、Brew Git、Brew recovery 测试集合；零测试成功、缺失测试或 ignored 漂移均失败。
三个 PG consumer 精确验证 outbox envelope/payload/schema，不再仅验证行数。


有序事件使用固定 RSS revision 的公开 Outbox 函数。全新数据库依次安装组件
`MIGRATION_SQL` 和 `OUTBOX_MIGRATION_SQL`；后者移除直接 INSERT/sequence 权限，
授予 prepare/append 函数 EXECUTE。产品已提交初始 SQL 不改写。
独立 execute/create/transition 在自己的事务入口调用 `prepare_outbox_partitions`。
组合事务由最外层宿主先收集各 store 的 `partition(id)`，一次声明完整集合，再取得业务锁；
内部 `*_in` 和事件 append 不重复声明。一次事务可以在同一分区追加多个事件。
Projection 启动使用调用方的 `Control`，pool、产品准入与组件准入共享绝对截止点及取消信号。
新安装只接受空 ledger 或与当前完整 manifest 一致的已完成 ledger；不导入或续接旧库。
