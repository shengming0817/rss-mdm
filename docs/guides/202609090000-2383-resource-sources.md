# Resource 与 WinGet/Brew 后端元数据

适用：#2383/#2384/#2385 的三个独立 Rust package。公共入口分别位于 `crates/resource`、
`crates/winget-source`、`crates/brew-source`。本批实现不包含审批、PG、API、远端发布或端侧操作。

## Resource

`Resource::new` 创建租户范围聚合；`Version::new` 校验并冻结 software/script/configuration 内容，
`insert` 对同身份同内容返回 `false`，冲突拒绝。一个版本最多 64 个精确平台/架构/variant；
支持 Windows/macOS、x86_64/aarch64。声明持有 executor/schema/检测等身份及稳定产物引用、长度、SHA-256；
没有执行接口。`Artifact::verify` 才比较实际传入字节，构造成功不证明对象存在。

版本标签为有界不透明身份，不按 SemVer 排序。V1 摘要使用域分隔、tenant、长度前缀、闭合标签、
变体数量及排序后的内容；凭据不进入摘要。私有字段和统一构造器保护冻结内容，不提供 Deserialize 或内容 setter。

Frozen → Active → Deprecated；Deprecated 可再次 Active，激活一个版本时旧 Active 同时 Deprecated。
完整且同 tenant/resource/version、引用数为零时可归档任何非删除版本；Archived 为终态，内容仍可读取。
不提供物理删除。所有转换显式输入 `Timepoint`，拒绝时间倒退。
`References` 是受控调用方提供的事实，不是认证或并发证明；未来 PG/组装必须在同一受控事务中保护引用检查。
`AccessBinding` 独立持有源与产物凭据引用，Debug 脱敏，不参与版本身份。

## WinGet 支持矩阵

仅 REST contract 1.0.0：`GET /information` 和
`GET /packageManifests/{PackageIdentifier}?Version={exact-version}`，带 `Version: 1.0.0`。
源必须明确声明支持该版本，SourceIdentifier 与配置精确一致；不自动降级、搜索公共源或调用 winget。

- installer：MSI/EXE；架构：x64/arm64；scope：user/machine/未声明。
  未声明只能被 `Scope::Unspecified` 匹配，不默认成为 machine。
- 可用 `Query::with_installer_id` 指定 InstallerIdentifier；未指定仍须唯一匹配，多个结果返回 Ambiguous。
- 单一目标版本、空 channel、默认 locale、无额外 locale；支持 installer 的身份、类型、架构、scope、URL 和 SHA-256。
  switches/dependencies/其他行为字段和未知协议明确 Unsupported；不声称支持完整 WinGet manifest 集合。
- URL 为无内嵌身份、query 或 fragment 的 HTTPS 地址。短期签名 URL 不进入持久元数据。
- `parse_manifest` 验证格式及精确身份；`verify_expected_digest` 比较调用方提供的期望摘要；
  `publication_metadata` 只返回受控发布者可消费的 REST-shaped JSON，不执行外部提交，也不证明下载字节。

`Source::new` 接受受信 tenant、精确源身份、以 `/` 结尾的 base URL、经批准的连接 IP 和源凭据引用。
地址由 `reqwest` resolver 固定，禁止重定向/隐式代理；HTTPS 模式拒绝 loopback/link-local/multicast/unspecified。
内部私有 IP 可由产品显式批准。LoopbackHttp 仅供显式本地 fixture；产品不能把外部不可信配置直接传入。
`Access` 在调用时注入源 token，必须匹配 tenant/source/credential reference；此结构不认证调用者。
源 token 不转给产物 URL，本批没有产物下载。凭据解析、地址批准和真实授权由后续组装拥有。
默认连接 5 秒、整个查询 30 秒、每响应 4 MiB；`with_limits` 只允许有界配置。错误不携带响应正文或 URL。

## Brew 支持矩阵与本地 Git

只支持完整 `owner/tap/name`、受控 Formula/Bottle 和 `app`/`pkg` Cask；不解析用户 Ruby。
Formula 固定从源归档安装一个命名可执行文件，Bottle 标签仅 `sonoma`/`arm64_sonoma`，
所有 Bottle 使用同一 HTTPS root_url，不声称 `any_skip_relocation`。依赖采用同租户完整 Tap/package 引用，
拒绝重复和自引用；依赖是否已批准及其快照由调用方决定。
Cask 支持 Arm64/Intel 的 URL+SHA-256，单架构显式生成 `depends_on arch`。
模板转义引号、反斜杠与 Ruby 插值，拒绝 hook、任意 DSL、路径穿越、动态代码及无摘要产物。
渲染顺序稳定，结果为私有构造的 `Document`，最大 1 MiB。

`Repository::open` 仅接受调用方明确授权、与 tenant/Tap 唯一映射的专用本地 SHA-1 bare 仓库；
调用方拥有目录访问控制，库不从目录参数推断认证权威。不得将不同租户映射到同一目录。
不支持工作区 checkout、外部共享 Tap、远端 clone/push 或自动仓库发现。

1. `prepare(base, document, operation, at)` 写 blob/tree/commit，但不更新 ref。
   固定 parent、document、operation、tenant/Tap 与时间可重建同一目标 commit；准备结果暴露 `target()`。
2. `apply(prepared)` 对 `refs/heads/main` 按预期 parent 作 CAS；已是目标返回 AlreadyApplied，
   分支漂移返回 Conflict。使用 no-deref，拒绝符号 ref，不跟随它覆写其他分支。
3. 提交结果未知时保留原输入与目标，重建原 Prepared 后查询/重放；禁止换 operation 或 parent 盲目重试。
   后续持久化恢复由 #2389 负责，本批不自建发布日志。
4. `read(commit, expected_document)` 固定完整 commit，核对路径 mode、blob 字节和内容摘要；
   分支前移不改变旧快照。Snapshot 是核对结果，不是远端发布回执。

使用 `/usr/bin/git` 参数调用，禁 hooks、懒加载、全局配置和隐式环境，单次命令 10 秒预算；
超预算输出终止并回收子进程。产物/Tap 凭据分别仅保留引用。库不执行 Ruby、brew 或安装动作。
源撤回、产物保留和外部结果对账属于 #2386/#2389。

## 验证命令与证据

```sh
cargo test --locked -p rss-mdm-resource -p rss-mdm-winget-source -p rss-mdm-brew-source
make source-t2
# 源码提交后，从固定 HEAD 构建三个独立的仓外 consumer
make source-consumers
# 最终本地验证，统一收集失败
make ci
```

T2 测试在普通 test 中显式 ignored，由 `source-t2` 用 `--ignored` 执行真实 HTTP socket 与本地 Git。
`make ci` 同时运行原有 PG T2、新接缝和独立 consumer。只运行普通 cargo test 不代表 T2 已执行。
consumer 重用所属 crate 的公共 API 行为测试，每个独立 workspace/lock/config/target，仅依赖一个产品 package；
外部值类型直接引用 canonical RSS owner。验证完整源码 SHA、RSS revision、依赖闭包和 feature tree，
不靠父仓 path/patch 或其他产品核心补齐依赖。当前没有可选产品 feature，因此只验证默认组合。

生成记录位于 `artifacts/source-consumers/` 和 `artifacts/local-ci/`，执行目录可再生，测试与脚本随源码提交。
运行结果以相应固定 HEAD 的实际日志为准，不把本指南当作通过证明。
无 registry 发布、真实兼容源部署、客户端安装/卸载或 T3 证据；本批没有独立性能 SLO，benchmark gate 不适用。

## 源码对标与 fixtures

- Microsoft winget-cli `c17eadbcacf2f5244b13de0d563d921c33ac4af6`：
  `src/AppInstallerRepositoryCore/Rest/Schema/1_0/RestInterface_1_0.cpp`、
  `Json/ManifestDeserializer_1_0.cpp` 和 `src/AppInstallerCLITests/RestInterface_1_0.cpp`。
  [固定源码](https://github.com/microsoft/winget-cli/tree/c17eadbcacf2f5244b13de0d563d921c33ac4af6/src/AppInstallerRepositoryCore/Rest/Schema/1_0)。
  `upstream-exe.json` 原样提取 GetGoodManifest_RequiredFields 的 JSON；information 原样取自官方 E2E fixture。
- Homebrew/brew `4ef2edc123233e30f94c4a6d21bcb019806f1945`：
  [Bottles](https://github.com/Homebrew/brew/blob/4ef2edc123233e30f94c4a6d21bcb019806f1945/docs/Bottles.md)、
  [Cask Cookbook](https://github.com/Homebrew/brew/blob/4ef2edc123233e30f94c4a6d21bcb019806f1945/docs/Cask-Cookbook.md)。
- reqwest `de55373434f07f42926599dbb5a88550d8e55112` 的 `src/redirect.rs` 用于确认关闭重定向的语义；
  实际 Cargo 依赖版本以 lock 为准。

历史 WinMDM `src/internal/domain/resource/entity.go` 等只用于识别旧耦合与行为差异；
不移植旧 JSON detail、Policy 客户端、存储实现或兼容模型。
