# #2364 产品认证浏览器运行证据

## 固定输入与结论

MDM 源码 `500f391735a6222f4cab723dc75c645afd60c862`；Identity `7236f77414706ccf1ede95f62b48bb99b4b65104`；UI `a0e5ac61e98677ae96c6c80d3680d5f75c4fbbfb`（必要前置 [Web PR #1064](https://dev.azure.com/shengming0923/rss/_git/rss-web/pullrequest/1064)）。
实际 Chromium `148.0.7778.0`。完整 OCI/lock/schema/配置/CA/平台身份见 [result.json](2364-auth-t3/result.json)。

真实浏览器 19 项全部通过，且清理完成后才生成通过记录。固定候选 smoke 也通过，见 [smoke.json](2364-auth-t3/smoke.json)。本记录提交仅保存证据，不改变受测源码；产品完整 CI 结果另由 PR 绑定最终提交记录。

## 实际命令（秘密输入已脱敏）

工作目录 `/Users/shengming/Documents/code/rss/rss-mdm/worktrees/feature/2364-auth-t3`。

```sh
python3 hack/release.py --output artifacts/2364-candidate-7 --git-auth-header-file '<本次临时 0600 认证头文件>'
make t3-auth MDM_CANDIDATE="$PWD/artifacts/2364-candidate-7" MDM_WEB_IMAGE=rss-identity-web:2364-logout-final MDM_BROWSER_IMAGE=rss-identity-reference-tools:1062-final MDM_T3_OUTPUT="$PWD/artifacts/2364-run-7"
python3 hack/candidate_smoke.py --candidate artifacts/2364-candidate-7 --web-image rss-identity-web:2364-logout-final
```

UI 在其独立、干净 worktree 运行 `pnpm image:identity --tag rss-identity-web:2364-logout-final`；71 个认证 UI 测试、类型检查及相关 lint 通过。构建和运行使用 Docker 默认平台，报告的平台来自实际镜像。

## 脱敏请求、事件及恢复

完整记录用 gzip 无损保存以避免日志占满代码 diff：
[请求](2364-auth-t3/requests.json.gz)、[产品审计](2364-auth-t3/audit.json.gz)、[按容器分组日志](2364-auth-t3/product.log.gz)、[smoke 日志](2364-auth-t3/smoke.log.gz)。
解压后分别核对 result.json 的 requests_sha256/audit_sha256/log_sha256，以及 smoke.json 的 log_sha256；请求 requestId 对应审计 request_id 和产品日志 request_id，网关记录 upstream_request_id。

在自有 PG pause 后，产品查询返回 503；关键 Inventory 任务失败后进程退出，网关退出请求返回 502。修复后的完整 UI 保留结果未确认提示，未导航为退出成功。恢复 PG 并重启后验证原会话仍有效，再确认退出；预先已撤销会话始终拒绝。
Keycloak 停止后，本地登录、账户管理和资产查询继续通过。第二实例使用同 tenant、不同 instance，另行验证错误 tenant；cookie 作用域及强行重放均拒绝。

## 范围与历史处置

资产是带来源的合成读模型；授权、查询、管理写入、拒绝无效果和审计均经真实 MDM。publisher 验证 release_read 的授权接线，不宣称实际软件发布。没有 Windows/macOS 真机注册、命令或 wipe 证明，也没有生产容量承诺。
当前安装仅支持空库或精确重放。错配实例安装被拒绝且账户/账本摘要不变，不转换旧主体、不改旧审计、不导入旧会话。切换失败应停止新部署并由原 owner 恢复完整旧部署/一致备份；本项不代表真实历史环境已退役或生产回退已演练。

此前失败均未生成通过记录：非 root 网关临时目录、夹具权限/有效期、登录预算及 UI 退出提示问题修复后重新构建候选。最终唯一通过证据绑定上列源码和镜像，不复用早期通过标记。
